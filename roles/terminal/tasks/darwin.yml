#- name: install iterm2 from homebrew cask 
#  homebrew_cask: name=iterm2 state=present

- name: install kitty from homebrew cask 
  # Install Ghostty via Homebrew Cask (brew install --cask ghostty)
  homebrew_cask:
    name: ghostty
    state: present
